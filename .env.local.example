# .env.local — Copy to .env.local and adjust as needed

# Backend API URL (blank = use Next.js rewrites proxy to localhost:8000)
NEXT_PUBLIC_API_URL=

# Set to "true" to use embedded mock data (no backend needed)
NEXT_PUBLIC_USE_MOCK=false
